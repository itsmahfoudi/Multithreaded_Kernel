#ifndef MULTITHREADED_KERNEL_STRING_H
#define MULTITHREADED_KERNEL_STRING_H


int strlen(const char* str);
int strnlen(const char* str, int max_len);
int is_digit(char c);
int to_numeric_digit(char c);

#endif //MULTITHREADED_KERNEL_STRING_H
